.env
.env.*
!.env.example
data/
*.db
*.sqlite
__pycache__/
*.pyc
.venv/
node_modules/
dist/
*.egg-info/

# Logs
*.log

# Playwright / e2e test artifacts
.playwright-mcp/
e2e-test-login
login-page-*
chat-*.png
exec-*.png
e2e-*.png

# Memory / hook plugin runtime data
.remember/

# Windows Mark-of-the-Web sidecar files
*:Zone.Identifier
*Zone.Identifier

# Claude Code per-machine settings (share .claude/settings.json, ignore .local)
.claude/settings.local.json

# Eval outputs（每次跑都会变，靠 results.json.example 当模板）
benchmarks/*/results.json

# 每实例本地化的 skill 配置（含 API URL / 密钥；.example 是模板）
skills/*/config.json

# 每实例本地化的 skill 业务数据（CSV / JSON 等真实数据）
# 框架不关心 skills/ 下任何内容，部署方自行管理
skills/*/config/

