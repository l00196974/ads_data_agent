# 03 技术选型与理由（Tech Stack & Rationale）

> ⏳ **占位**：本篇骨架已建，内容待补。

**关联源码**：`requirements.txt`、`frontend/package.json`

**计划覆盖**：
- 选型表（Python 3.13 + FastAPI / Vue 3 + Vite / deepagents / LangGraph / ECharts）
- 为什么选 deepagents 而不是裸 LangGraph
- 为什么前端不用 EventSource 而手写 fetch+ReadableStream
- 为什么不用 Next.js / React（前端选型）
- 为什么 SSE 而不是 WebSocket
