# Core agent loop（P4b 后框架已删干净）
openai                # 直调 SDK (替代 langchain_openai.ChatOpenAI)
langchain-core        # 仅保留 BaseMessage/Human/AI/Tool/SystemMessage 数据类
aiosqlite             # ThreadStore 持久化

# Web layer
fastapi
uvicorn[standard]
python-multipart
python-dotenv
pyyaml
pydantic>=2.0

# Utils
tiktoken>=0.7         # 上下文 breakdown 估算（runner_v2 / channel 报 metrics 用）
